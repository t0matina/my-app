import React from 'react'

const VComponent = (props) => {
    return (
        <div>
            <h1>Hello from Arraow Component {props.firstName} {props.lastName}</h1>
        </div>
    )
}

export default VComponent;